<div class="container">
  <div class="theme">
    <%= @theme.theme %>
  </div>

  <%= form_with url:user_answer_path, model: @answer, local:true do |f| %>
  <div><%= f.text_area :answer %></div>
  <div><%= f.submit '回答する' %></div>
  <% end %>
</div>